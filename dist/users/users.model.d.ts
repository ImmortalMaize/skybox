export declare class User {
    publicId: number;
    alias: string;
    email: string;
    password: string;
    bio: string;
    constructor(publicId: number, alias: string, email: string, password: string, bio: string);
}
